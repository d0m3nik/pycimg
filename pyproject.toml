[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "pybind11>=2.6.0",
]

build-backend = "setuptools.build_meta"

[tool.semantic_release]
branch = "master"
version_pattern = "pycimg/__init__.py:__version__,setup.py:__version__"
