sudo: required

services: 
  - docker

env: DOCKER_IMAGE=d0m3nik/cython-primary:0.0.7

before_install:
  - docker pull $DOCKER_IMAGE

script:
  - docker run -v `pwd`:`pwd` -w `pwd` $DOCKER_IMAGE `pwd`/build_circleci.sh
  - ls dist/
