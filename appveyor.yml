environment:
  TWINE_USERNAME: d0m3nik
  CIBW_BEFORE_BUILD: pip install requirements_dev.txt && conan install .

build_script:
  - pip install cibuildwheel==0.10.0
  - cibuildwheel --output-dir wheelhouse

artifacts:
  - path: "wheelhouse\\*.whl"
    name: Wheels


# init:
#   - ps: iex ((new-object net.webclient).DownloadString('https://raw.githubusercontent.com/appveyor/ci/master/scripts/enable-rdp.ps1'))



    #test_script:
    #  - mkdir test-results
    #  - mkdir cover
    #  - "%PYTHON%\\python.exe setup.py nosetests"

on_success:
  # Deploy to PyPI on tag
  - >
    IF "%APPVEYOR_REPO_TAG%" == "true"
    (
    "%PYTHON%\\python.exe" -m pip install twine
    &&
    "%PYTHON%\\python.exe" -m twine upload wheelhouse/*.whl
    )

# vvv Enable for debugging on appveyor build worker vvv
#on_finish:
#  - ps: $blockRdp = $true; iex ((new-object net.webclient).DownloadString('https://raw.githubusercontent.com/appveyor/ci/master/scripts/enable-rdp.ps1'))
